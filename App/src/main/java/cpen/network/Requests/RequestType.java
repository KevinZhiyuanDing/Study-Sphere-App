package cpen.network.Requests;

/**
 * All possible request types.
 */
public enum RequestType {
    CREATE_SESSION,
    GET_SESSIONS,
    GET_ROOMS,
    ACCEPT_REQUEST,
    CREATE_USER,
    USER_LOGIN,
    JOIN_SESSION
}

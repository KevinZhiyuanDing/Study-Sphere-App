package cpen.network.Responses;

/**
 * All possible response types.
 */
public enum ResponseType {
    ACCEPT_SESSION,
    GET_ROOMS,
    GET_SESSIONS,
    CREATE_USER,
    LOGIN,
    JOIN_SESSION
}
